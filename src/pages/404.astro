---
import BaseLayout from '@/layouts/BaseLayout.astro';
import TopNav from '@/components/ui/TopNav.astro';
---

<BaseLayout
  title="Not Found / 砂糖さんの実験室"
  description="お探しのページは見つかりませんでした。"
>
  <TopNav current="home" />

  <section class="wrap">
    <div class="mono">— ERROR / 404</div>
    <h1>
      迷子に、<br />
      <span>なりましたか。</span>
    </h1>
    <p class="desc">
      お探しのページは、引っ越した、<br />
      あるいは最初から存在しなかったかもしれません。
    </p>

    <div class="actions">
      <a href="/" class="primary">
        <span class="label">索引へ戻る</span>
        <span class="arrow">→</span>
      </a>
      <div class="secondary">
        <a href="/fonts/">01 / Fonts</a>
        <a href="/motion/">02 / Motion</a>
        <a href="/branding/">03 / Branding</a>
      </div>
    </div>

    <div class="meta">
      <span>— SATO-LAB</span>
      <span>404 NOT FOUND</span>
    </div>
  </section>
</BaseLayout>

<style>
  .wrap {
    min-height: 80vh;
    padding: 80px 5vw;
    display: flex;
    flex-direction: column;
    justify-content: center;
    background: var(--bg);
    color: var(--text);
  }
  .mono {
    font-family: var(--font-brand-en);
    font-size: 11px;
    letter-spacing: 0.3em;
    color: var(--accent);
    margin-bottom: 24px;
    font-weight: 600;
  }
  h1 {
    font-family: var(--font-brand-heading);
    font-weight: 700;
    font-size: clamp(44px, 8vw, 104px);
    margin: 0;
    line-height: 1.1;
    letter-spacing: -0.02em;
  }
  h1 span {
    color: var(--accent);
  }
  .desc {
    font-family: var(--font-brand-body);
    font-size: 16px;
    line-height: 1.9;
    color: var(--text-secondary);
    margin-top: 32px;
    max-width: 540px;
  }
  .actions {
    margin-top: 64px;
    display: flex;
    align-items: center;
    gap: 40px;
    flex-wrap: wrap;
  }
  .primary {
    display: inline-flex;
    align-items: center;
    gap: 16px;
    padding: 18px 30px;
    border: 1px solid var(--accent);
    color: var(--accent);
    font-family: var(--font-brand-heading);
    font-size: 18px;
    font-weight: 700;
    border-radius: 999px;
    transition:
      background 0.25s,
      color 0.25s;
  }
  .primary:hover {
    background: var(--accent);
    color: var(--bg);
  }
  .primary .arrow {
    font-family: var(--font-brand-en);
    transition: transform 0.25s;
  }
  .primary:hover .arrow {
    transform: translateX(6px);
  }
  .secondary {
    display: flex;
    gap: 24px;
    flex-wrap: wrap;
    font-family: var(--font-brand-en);
    font-size: 12px;
    letter-spacing: 0.2em;
  }
  .secondary a {
    color: var(--text-secondary);
    padding: 8px 0;
    border-bottom: 1px solid transparent;
    transition:
      color 0.2s,
      border-color 0.2s;
  }
  .secondary a:hover {
    color: var(--accent);
    border-color: var(--accent);
  }
  .meta {
    margin-top: 96px;
    display: flex;
    justify-content: space-between;
    font-family: var(--font-brand-en);
    font-size: 10px;
    letter-spacing: 0.3em;
    color: var(--text-tertiary);
    flex-wrap: wrap;
    gap: 16px;
  }
</style>
